from abc import ABC, abstractmethod


class LLMClient(ABC):
    @abstractmethod
    def explain(self, transcript: str) -> str:
        """Принимает фрагмент транскрипта, возвращает объяснение."""
        raise NotImplementedError

    @abstractmethod
    def explain_image(self, image_bytes: bytes, context: str) -> str:
        """Принимает PNG-байты скриншота и текстовый контекст разговора,
        возвращает объяснение того, что на изображении."""
        raise NotImplementedError


EXPLAIN_SYSTEM_PROMPT = (
    "Ты помогаешь бэкенд-разработчику быстро отвечать на вопросы IT "
    " рекрутера для успешного прохождения собеседования,"
    " когда всплывают термины из Python разработки, ты должен дать прояснение "
    " Тебе дан фрагмен транскрипта, который может быть неидеально распознан"
    " Кратко и просто объясняй спрашиваемые термины, если они есть"
    " 2-3 предложения, без вступлений"
)

IMAGE_EXPLAIN_SYSTEM_PROMPT = (
    "Ты помогаешь бэкенд-разработчику разобраться в том, что показано "
    "на скриншоте с рабочего созвона или личной работы (диаграмма, макет "
    "дизайнера, график, схема архитектуры, задача с кодом и т.п.). "
    "Тебе также может быть дан текстовый контекст последних минут "
    "разговора — используй его, если он помогает, но не выдумывай связь, "
    "если её нет. Если на скриншоте вопрос, задача или проблема (например, "
    "задача по программированию) — реши её по существу и дай конкретный "
    "ответ или решение (с кодом, если это уместно), а не просто "
    "пересказывай условие. Если это просто иллюстрация/диаграмма без "
    "явного вопроса — кратко объясни, что на ней. Отвечай по-русски."
)